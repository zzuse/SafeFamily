# Agent rules (Codex)

## Do not read or modify sensitive or auto-generated files
- NEVER open/read `.env` file.
- NEVER open/edit `styles.css` unless I explicitly ask you to.

## Safe defaults
- If a task might require `.env`, ask me to paste only the specific values needed (redacted if possible).
- Prefer `.env.example` and environment variables for configuration.
